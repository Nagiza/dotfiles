Replace a word/frase/single char from previous command and run the modified again replacing foo with bar
^foo^bar

Run previous command, usually used for forgotten sudo.
!!

Run part of previous command, base 0
!:'number'

Install package with the suggested packages as dependancies*.
sudo apt-get install --install-suggests <packageName>

Find a file ending in .iso from . recursivly
find . -name '*.iso'

write iso file to Usb.
dd bs=4M if=/path/to/archlinux.iso of=/dev/sdx && sync

How to detect keyboard input
xev

Commands for audio control
amixer -D pulse sset Master 5%+
amixer -D pulse sset Master 5%-
amixer -D pulse sset Master toggle
